\ProvidesPackage{preamble}

\usepackage[margin=1cm]{geometry}
\geometry{a3paper}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{graphicx}
\usepackage{multicol}
\usepackage{xcolor}
\usepackage{mathtools}

% subfiles 
\usepackage{subfiles}

% No Page Number
\pagenumbering{gobble}
% No Indent 
\setlength\parindent{0pt}
% Line Spacing to 1 
\renewcommand{\baselinestretch}{1.0}
% Remove line spacing before and after sections 
\usepackage{titlesec}
\titlespacing\section{0pt}{0pt plus 0pt minus 0pt}{0pt plus 0pt minus 0pt}
\titlespacing\subsection{0pt}{0pt plus 0pt minus 0pt}{0pt plus 0pt minus 0pt}
\titlespacing\subsubsection{0pt}{0pt plus 0pt minus 0pt}{0pt plus 0pt minus 0pt}

\renewcommand{\familydefault}{\sfdefault}

%\AtBeginEnvironment{equation*}
%{\setlength{\abovedisplayskip}{0pt}\setlength{\belowdisplayskip}{0pt}\color{black}}
%\AtBeginEnvironment{gather*}
%{\setlength{\abovedisplayskip}{0pt}\setlength{\belowdisplayskip}{0pt}\color{black}}
%\AtBeginEnvironment{align*}
%{\setlength{\abovedisplayskip}{0pt}\setlength{\belowdisplayskip}{0pt}\color{black}}
%\let\oldeqs\equalion*l
%\renewenvironment{equation*}[0]{\begin{equation*}}{\end{equation*}}
%\newenvironment{equation*}{\color{black}\equation*}{\endequation*}
%\newenvironment{align*}{\color{black}\align*}{\endalign*}
% make sum, intersection, union, integral bigger

\let\smallsum\sum
% \renewcommand{\sum}{\displaystyle\smallsum}
\let\smallprod\prod
% \renewcommand{\prod}{\displaystyle\smallprod}
\let\smallint\int
% \renewcommand{\int}{\displaystyle\smallint}
\let\smallbigcup\bigcup
% \renewcommand{\bigcup}{\displaystyle\smallbigcup}
\let\smallbigcup\bigcap
% \renewcommand{\bigcap}{\displaystyle\smallbigcup}

% Some other symbol
\newcommand{\tf}{$\Rightarrow$ }

% remove space before multicol 

\setlength{\multicolsep}{0pt}
% \setlength{\columnseprule}{0.2pt}
% \def\columnseprulecolor{\color{black}}

% reduce list spacing
\usepackage{enumitem}
\setlist{nosep}

% Colors


% Color tweaks

\newcommand{\marker}[1]{{\color{black}{\textbf{#1}}}}

% set section color to black 
\let\oldsection\section
\renewcommand{\section}[1]{\oldsection{\color{black}{#1}}}
\renewcommand{\thesection}{\color{black}{\arabic{section}}}

\let\oldsubsection\subsection
\renewcommand{\subsection}[1]{\oldsubsection{\color{black}{#1}}}
\renewcommand{\thesubsection}{\color{black}{\arabic{section}.\arabic{subsection}}}

% Change color of equation to black 
\let\oldinlinemathstart\(
\renewcommand{\(}{\begingroup\color{black}\oldinlinemathstart}
\let\oldinlinemathend\)
\renewcommand{\)}{\oldinlinemathend\endgroup}

% Itemize 

\usepackage{enumitem}
\setlist[itemize]{
  before=\small,
  label=\tiny\textcolor{black}{\textbullet},
  leftmargin=*,labelsep=0.3em
}
\setlist[enumerate]{
  before=\small,
  label=\small\textcolor{black}{\arabic*.},
  leftmargin=*,labelsep=0.3em
}

